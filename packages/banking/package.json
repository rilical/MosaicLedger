{
  "name": "@mosaicledger/banking",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/demo && cp src/demo/*.json dist/demo/",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests'"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
